plugins {
    id "com.android.application"
}

android {
    namespace "com.warmcountdown.app"
    compileSdk 36

    buildFeatures {
        buildConfig true
    }

    defaultConfig {
        applicationId "com.warmcountdown.app"
        minSdk 26
        targetSdk 36
        versionCode 24
        versionName "1.24"
    }
}
