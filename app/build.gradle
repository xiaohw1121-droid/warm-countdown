plugins {
    id "com.android.application"
}

android {
    namespace "com.warmcountdown.app"
    compileSdk 36

    defaultConfig {
        applicationId "com.warmcountdown.app"
        minSdk 26
        targetSdk 36
        versionCode 1
        versionName "1.0"
    }
}
